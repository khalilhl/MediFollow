apiVersion: v1
kind: Service
metadata:
  name: medifollow-backend-service
spec:
  type: NodePort
  selector:
    app: medifollow-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000
  