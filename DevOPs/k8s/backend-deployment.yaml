apiVersion: apps/v1
kind: Deployment
metadata:
  name: medifollow-backend-deployment
  labels:
    app: medifollow-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: medifollow-backend
  template:
    metadata:
      labels:
        app: medifollow-backend
    spec:
      containers:
      - name: medifollow-backend
        # This pulls directly from the user's DockerHub repository
        image: rahmaaaaa/medifollow-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: medifollow-secrets
